import com.mycompany.blog.UserAdd;
import org.junit.jupiter.api.Test;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import org.mockito.Mockito;
public class UserAddTest {
/*

    @Test
    public void testProcessRequest() throws Exception {
        // Mock HttpServletRequest, HttpServletResponse, and HttpSession
        HttpServletRequest request = Mockito.mock(HttpServletRequest.class);
        HttpServletResponse response = Mockito.mock(HttpServletResponse.class);
        HttpSession session = Mockito.mock(HttpSession.class);

        // Set up the request parameters
        Mockito.when(request.getParameter("name")).thenReturn("John");
        Mockito.when(request.getParameter("email")).thenReturn("john@example.com");
        Mockito.when(request.getParameter("post")).thenReturn("Hello, world!");

        // Set up the HttpSession
        Mockito.when(request.getSession()).thenReturn(session);

        // Create an instance of UserAdd
        UserAdd userAdd = new UserAdd();

        // Call the processRequest method
        userAdd.processRequest(request, response);

        Mockito.verify(response).sendRedirect("./");
    }

*/

}
